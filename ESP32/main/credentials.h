// Placeholder Wi‑Fi credentials. Replace the values of WIFI_SSID and
// WIFI_PASSWORD with your own network name and password before building
// the firmware. 

#pragma once

namespace Credentials {
  constexpr const char WIFI_SSID[]     = "your-ssid";
  constexpr const char WIFI_PASSWORD[] = "your-password";
}